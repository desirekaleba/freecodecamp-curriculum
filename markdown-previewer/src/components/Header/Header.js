import React from 'react';
import './Header.css';

const Header = (props) => {
    return (
        <header className="container">
            <h1>Markdown Previewer</h1>
        </header>
    );
};

export default Header;